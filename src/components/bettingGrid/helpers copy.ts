import {
    GridWidth, GridHeight, Straights, StraightsRows, Dozens, Colums,
    BottomCells, ColumnWidth, ZeroWidth,
} from './constans'
export { }

const StraightWidth = GridWidth / (ZeroWidth + Straights + ColumnWidth)